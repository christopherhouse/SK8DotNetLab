apiVersion: apps/v1
kind: Deployment
metadata:
  name: sk8dotnet-api
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sk8dotnet-api
  template:
    metadata:
      labels:
        app: sk8dotnet-api
    spec:
      containers:
      - name: sk8dotnet-api
        image: sk8dotnet-api:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: sk8dotnet-api-service
spec:
  selector:
    app: sk8dotnet-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer